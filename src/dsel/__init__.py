"""dsel — a datastore selection decision harness.

GATE -> MEASURE -> WEIGH. This is a decision harness, not a benchmark suite: it
answers "which of {X, Y, Z} should we build on?", so it optimises for fair,
comparable, ordinal numbers rather than absolute peaks.
"""

__version__ = "0.1.0"

__all__ = ["__version__"]
